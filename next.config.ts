import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    domains: ["res.cloudinary.com"], // Agrega aquí el dominio permitido
  },
  /* otras opciones de configuración */
};

export default nextConfig;
